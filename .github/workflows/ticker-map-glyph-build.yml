name: Ticker map glyph workspace
on:
  push:
    branches: [codex/ticker-map-glyph-build]
permissions:
  contents: read
jobs:
  source:
    runs-on: ubuntu-latest
    timeout-minutes: 3
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1
        with:
          ref: 14582905549c76ee9b0f2caf0cad47ad389d4af0
      - run: git archive --format=tar.gz HEAD > /tmp/ticker-map-source.tar.gz
      - uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a
        with:
          name: ticker-map-source
          path: /tmp/ticker-map-source.tar.gz
          retention-days: 1
