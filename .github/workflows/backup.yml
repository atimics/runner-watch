name: Fly Volume Backup

on:
  schedule:
    - cron: "17 09 * * *"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  snapshot:
    name: Snapshot production data
    runs-on: ubuntu-latest
    steps:
      - uses: superfly/flyctl-actions/setup-flyctl@master
      - run: flyctl volumes snapshots create vol_rnz3gwm5m9em2q1r --app runner-watch-db
        env:
          FLY_API_TOKEN: ${{ secrets.FLY_API_TOKEN }}
