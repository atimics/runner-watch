name: Rust Dependency Audit

on:
  pull_request:
    paths:
      - ".github/workflows/rust-audit.yml"
      - "desktop/src-tauri/Cargo.lock"
      - "rust/stonks-ranker/Cargo.lock"
  schedule:
    - cron: "19 8 * * 1"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  changes:
    name: Check new Rust advisories
    if: github.event_name == 'pull_request'
    permissions:
      actions: read
      contents: read
      security-events: write
    uses: google/osv-scanner-action/.github/workflows/osv-scanner-reusable-pr.yml@8ac9e5ce44cc7178e0e04229a91bdcc003166e57
    with:
      scan-args: |-
        --lockfile=./rust/stonks-ranker/Cargo.lock
        --recursive
        ./desktop
      upload-sarif: false
      fail-on-vuln: true

  full:
    name: Check all Rust advisories
    if: github.event_name != 'pull_request'
    permissions:
      actions: read
      contents: read
      security-events: write
    uses: google/osv-scanner-action/.github/workflows/osv-scanner-reusable.yml@8ac9e5ce44cc7178e0e04229a91bdcc003166e57
    with:
      scan-args: |-
        --lockfile=./rust/stonks-ranker/Cargo.lock
        --recursive
        ./desktop
      upload-sarif: false
      fail-on-vuln: true
