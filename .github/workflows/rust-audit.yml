name: Rust Dependency Audit

on:
  pull_request:
    paths:
      - ".github/workflows/rust-audit.yml"
      - "desktop/src-tauri/Cargo.lock"
      - "desktop/backport-regression/Cargo.lock"
      - "rust/stonks-ranker/Cargo.lock"
  schedule:
    - cron: "19 8 * * 1"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  changes:
    name: Check new Rust advisories
    if: github.event_name == 'pull_request'
    permissions:
      actions: read
      contents: read
      security-events: write
    uses: google/osv-scanner-action/.github/workflows/osv-scanner-reusable-pr.yml@8e5cf47b818121e8b405931c82126c2630b0b20d
    with:
      scan-args: |-
        --lockfile=./desktop/src-tauri/Cargo.lock
        --lockfile=./desktop/backport-regression/Cargo.lock
        --lockfile=./rust/stonks-ranker/Cargo.lock
      upload-sarif: false
      fail-on-vuln: true

  full:
    name: Check all Rust advisories
    if: github.event_name != 'pull_request'
    permissions:
      actions: read
      contents: read
      security-events: write
    uses: google/osv-scanner-action/.github/workflows/osv-scanner-reusable.yml@8e5cf47b818121e8b405931c82126c2630b0b20d
    with:
      scan-args: |-
        --lockfile=./desktop/src-tauri/Cargo.lock
        --lockfile=./desktop/backport-regression/Cargo.lock
        --lockfile=./rust/stonks-ranker/Cargo.lock
      upload-sarif: false
      fail-on-vuln: true
